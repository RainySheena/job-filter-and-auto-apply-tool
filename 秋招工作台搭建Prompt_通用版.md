# 秋招投递作战工作台 — 最终一键搭建 Prompt

> 直接复制以下 Prompt 发给任意支持网页生成、定时任务和 API 调用的 Agent，用于一次性搭建当前版本的秋招岗位投递作战工作台。
> 请先把所有 `[占位符]` 替换为你自己的信息。不要把飞书 App Secret、群 ID、手机号、简历文件等敏感信息写入公开网页代码。

---

## 角色

你是一名全栈开发工程师，使用 Vibe-Coding 模式，帮我搭建一个可公开访问、可持续自动更新的秋招岗位投递作战工作台。要求先交付可预览网页 Demo，确认后发布到目标网页托管平台，并配置每日自动岗位采集和飞书日报推送。

必须严格遵守：**岗位数据只能来自真实可核验来源，禁止编造公司、岗位、发布时间、截止时间、投递链接或 JD。无法确认的信息统一标记为「待核验」。**

## 零、平台限制硬约束（必须先读）

如果目标 Agent / 托管平台不支持在线数据库，或动态页面中的服务端文件存储（例如 `XDG_DATA_HOME`、本地 JSON 文件、实例内临时文件）是**实例级隔离**的，那么不同运行实例、冷启动实例、定时任务实例和网页访问实例之间可能不共享文件系统。

因此必须明确：
- **不要把目标托管平台动态页面的服务端 JSON 文件当作数据库。**
- **不要承诺 `/api/state`、`/api/jobs` 这类接口天然支持跨设备、跨实例、长期持久化。** 如果这些接口背后只是实例本地文件，它们只能作为实例存活期内的临时缓存。
- **用户操作数据**（投递进度、待投递清单状态、目标公司、自定义列、备注、JD 编辑）在不接外部数据库时，只能可靠保存在当前浏览器 `localStorage`，并通过 CSV 导入/导出做人工备份和迁移。
- **跨设备同步或云端持久化必须接外部持久数据源**，例如飞书多维表格、飞书电子表格、GitHub 仓库/Gist、Supabase、Firebase、Vercel KV 等。没有外部持久数据源时，不要声称可以跨设备实时同步。
- **定时任务是 isolated session**，每次执行可能在全新的工作区里运行，不能依赖原会话里的 `/workspace/.../public/index.html` 路径，不能假设可以直接修改原 HTML 文件并重新发布。
- **定时任务更新网页数据时，不能再走“修改旧 session 的 HTML 文件 + 重新发布”链路。** 定时任务每次可能运行在不同 isolated session，找不到原工作区里的 `public/index.html`，这会导致“WonderCV 抓取成功、飞书推送成功，但网页端仍是旧岗位”。正确做法是：定时任务抓取岗位后直接 POST 到线上服务器 API 或写入外部持久岗位数据源，前端启动时从同一数据源/API 加载最新岗位；如果做不到，要明确说明“飞书日报可更新，但网页端无法可靠自动更新”。
- **今日新增不能用 WonderCV 原始发布时间 `publish` 判断。** WonderCV 的 `publish` 可能是平台收录日期或岗位原发布时间（如 8月8日、8月12日），不等于本次定时任务新增日期。服务端/定时任务接收岗位数据时必须自动写入 `scraped_date`（本次采集日期，格式 `YYYY-MM-DD`），前端 `isNew` 和「今日新增」板块必须基于 `scraped_date` 判断过去 24 小时新增，岗位总库仍保留 `publish` 作为原始发布时间字段。

## 一、个人背景（用于岗位筛选与匹配说明）

- 姓名：[你的姓名]
- 学历：[如：XX大学 统计学硕士 / XX大学 经济学本科]
- GPA：[如：4.0/4.0，排名 X/Y]
- 实习经历：[如：字节跳动-数据科学]
- 核心技能：[如：Python / SQL / R / Spark / A/B 实验 / 因果推断 / 机器学习 / 统计检验]
- 毕业年份：[如：2027届]
- 投递方向：[如：互联网数据分析 / 数据科学 / 数据挖掘]
- 意向城市：[如：北京]
- 城市硬约束：只收录工作城市/base 包含 `[意向城市]` 的岗位；其他城市一律剔除。若一个岗位城市列表同时包含意向城市和其他城市，可以保留，但必须在城市字段完整展示全部城市。

## 二、网页整体要求

搭建单页工作台，采用 **顶部导航 Tab + 分屏面板切换** 布局。点击导航 Tab 切换面板，同一时间只展示一个板块。

页面顶部必须包含：
- 标题：秋招投递作战工作台
- 当前日期与秋招状态，例如：`YYYY-MM-DD · 秋招进行中`
- 个人背景概览栏：姓名、学历、GPA、技能标签、实习经历、投递方向、意向城市
- 风格：简洁清爽的求职工具风，白底、浅色卡片、清晰表格、链接可点击跳转，适配电脑浏览器

必须包含 6 个 Tab：
1. 今日新增
2. 岗位总库
3. 待投递清单
4. 投递进度管理
5. 目标公司池
6. 数据复盘面板

## 三、功能模块

### 1. 今日新增岗位

展示过去 24 小时新收录或更新的秋招岗位。

必须包含：
- 统计卡片：今日新增岗位数、其中可投数、涉及公司数
- 筛选器：公司类型、招聘批次
- 表格字段：公司名称、岗位名称、工作城市、招聘批次、发布时间、投递截止、官网链接、岗位状态
- 新增岗位在公司名旁标记 `NEW`
- 官网链接按钮文案为 `去投递 ↗`
- 若截止时间未知，展示 `待核验`，不要生成虚假的截止日期
- 今日新增判定必须使用 `scraped_date`，即本次定时任务采集/写入日期；禁止使用 WonderCV 原始 `publish` 字段判断是否为今日新增

### 2. 岗位总库

展示全部收录岗位。岗位总库必须来自同一个权威岗位数据源。首版可用 HTML 内嵌 `JOBS_DATA` 作为兜底数据；若配置了外部持久数据源或可靠的线上岗位 API，则页面加载时应先渲染内嵌数据，再异步拉取最新岗位数据覆盖渲染。

筛选器必须支持：
- 公司
- 公司类型
- 城市
- 招聘批次
- 岗位状态
- 重置筛选

表格字段必须为：
公司名称 | 公司类型 | 岗位名称 | 工作城市 | 背景匹配 | 官网链接 | 发布时间 | 投递截止 | 信息来源 | 岗位状态

功能要求：
- 支持导出岗位总库 CSV
- 所有链接必须指向真实可核验来源：WonderCV 详情页、WonderCV 可检索列表页、公司官网招聘页或用户确认的真实岗位页面
- `背景匹配` 列必须说明岗位与个人背景的具体契合点，例如「统计学+机器学习契合」「因果推断+A/B 实验契合」「经济金融+统计背景契合」
- 若岗位方向与个人背景不匹配，直接剔除，不要为了凑数量保留
- 页面说明文案必须明确：仅收录 base 在 `[意向城市]` 的岗位，数据来自真实抓取或用户确认，无法确认则标记「待核验」
- 今日新增、岗位总库、待投递清单必须从同一份岗位数据渲染，不能出现飞书日报有新增岗位但网页端岗位总库和今日新增不一致的情况
- 岗位数据对象必须同时保留 `publish` 和 `scraped_date`：`publish` 用于展示原始发布时间/平台收录日期，`scraped_date` 用于今日新增和定时任务同步判断

### 3. 待投递清单

从岗位总库中自动筛出未处理、未截止、仍可跟进的岗位，按截止时间升序展示；截止时间为 `待核验` 的岗位排在最后。

必须包含：
- 统计卡片：待投递总数、3 天内截止数、最近截止岗位
- 卡片式列表：公司名称、岗位名称、城市、倒计时、公司类型标签、投递链接
- 倒计时规则：3 天内红色预警，7 天内橙色提醒，其余普通展示；`待核验` 显示「截止时间待核验」
- 每张卡片有三个操作：`不投递`、`已投递`、`去投递 ↗`
- 点击 `不投递` 后，该岗位从待投递清单移除，并记录为不投递状态
- 点击 `已投递` 后，该岗位从待投递清单移除，并自动加入投递进度管理；分类根据公司类型自动归入「央国企」或「互联网」

### 4. 投递进度管理

这是核心模块。必须做成双表分区：
- 央国企投递
- 互联网投递

表格基础字段：
公司名称 | 岗位信息 | JD | 投递时间 | 目前进度 | 笔试 | 1面 | 2面 | 操作

字段和交互要求：
- `岗位信息` 支持填写完整岗位名称和岗位编号，例如 `集团公司-科技运营岗-2027届校招(J78974)`
- `JD` 以按钮 `📄 查看JD` 展示，点击打开弹窗，可查看和编辑完整 JD 文本
- `目前进度` 是下拉框，选项固定为：简历初筛、笔试、面试、OC、挂掉
- 投递时间、笔试、1面、2面等字段支持内联编辑
- 操作列包含：编辑、复制、删除
- 支持 `+ 添加投递记录`，弹窗字段至少包含：公司分类、公司名称、岗位信息、投递时间、目前进度、JD
- 添加记录时可选择分类：央企国企 / 互联网，自动归入对应表
- 支持 `+ 添加列`，用户可添加自定义列；表头展示列名并允许删除自定义列
- 支持导出投递记录 CSV
- 支持导入投递记录 CSV
  - 导入分类选项：使用 CSV 中的分类（无则默认央国企）、全部导入为央国企、全部导入为互联网
  - 导入方式：覆盖导入、增量添加
  - 增量添加按 `公司名称 + 岗位信息` 去重
- 支持行拖拽排序或至少保留拖拽手柄视觉位
- 数据保存硬约束：默认只承诺浏览器 `localStorage` 单设备持久化，并提供 CSV 导出/导入作为备份和迁移手段。不要声称目标托管平台的实例级服务端文件或 `/api/state` 能实现跨设备云同步；如需云同步，必须先接入外部持久数据源。

### 5. 目标公司池

卡片式管理重点关注公司。

必须包含：
- 添加公司输入框和 `添加公司` 按钮
- 每张公司卡片展示：公司名称、关注理由、岗位库收录数量、可投数量、删除按钮
- 岗位库收录数量和可投数量必须自动从当前岗位数据源联动计算；首版可从 HTML 内嵌 `JOBS_DATA` 计算，接入外部持久岗位数据源后必须从最新岗位数据计算
- 允许保留目标公司即使岗位库当前没有收录岗位，用于后续持续追踪
- 默认目标公司可按个人背景初始化，例如互联网数据科学目标、央国企金融科技目标、统计/精算/量化相关目标

### 6. 数据复盘面板

随岗位库和投递记录实时更新。

核心指标：
- 收录岗位数
- 已投递数
- 面试率
- OC 数
- 已截止数

图表和列表：
- 各阶段岗位分布条形图：简历初筛、笔试、面试、OC、挂掉
- 岗位状态占比环形图或圆环式视觉组件：可投、待核验、已截止
- 投递漏斗图：收录岗位 → 已投递 → 进入笔试/面试 → 拿到 OC
- 关键转化率列表：投递率、笔试/面试率、OC 率、流程终止占比

## 四、数据来源与采集规则

岗位数据优先来自 **超级简历 WonderCV 校招信息汇总** 实抓：
- 央企国企：`https://www.wondercv.com/xiaozhao/?graduate_year=[毕业年份]及以后&company_nature=央企国企`
- 全量/互联网：`https://www.wondercv.com/xiaozhao/?graduate_year=[毕业年份]及以后`

可以补充的真实来源：
- 公司官网招聘页
- 用户明确确认的真实岗位信息
- 用户后续提供的 CSV、飞书表格或多维表格数据

硬性采集规则：
1. 只收录 `[毕业年份]` 及以后的秋招、校招、提前批岗位。
2. 批次字段统一为 `[毕业年份]秋招` 或 `[毕业年份]秋招提前批`。
3. 只保留工作城市/base 包含 `[意向城市]` 的岗位，其他城市一律剔除。
4. 根据 `公司名称 + 岗位名称 + 工作城市 + 招聘批次` 四个维度去重。
5. 每条岗位尽量抓取：公司名称、公司类型、岗位名称、工作城市、背景匹配、官网链接、发布时间、投递截止、信息来源、岗位状态。
6. 每条由定时任务写入的岗位必须追加 `scraped_date` 字段，值为本次采集日期 `YYYY-MM-DD`；该字段不等同于 `publish`。
7. 无法确认投递有效性的岗位，状态标记为「待核验」。
8. 无法确认截止时间，截止字段填「待核验」。
9. 禁止用经验补全、编造、猜测或美化岗位名称。例如 WonderCV 原文没有写「数据分析」时，不能强行改成「数据分析岗」。
10. 大厂官网如果是 JavaScript 动态渲染、无法直接抓取岗位列表，不要编造岗位；改用 WonderCV 可核验信息或标记「待核验」。
11. 如果 WonderCV 详情页不可访问，但 WonderCV 列表页可以检索到公司和岗位线索，可以链接到可检索列表页，并将状态标为「待核验」。
12. 按个人背景筛选匹配方向，保留：
    - 数据分析
    - 数据科学
    - 数据挖掘
    - 统计学
    - 量化
    - 金融科技
    - 投资研究
    - 管培生（科技/金融/数据方向）
    - 精算
    - AI 算法
    - 大数据 / 数据仓库 / 数据架构
13. 剔除不匹配方向，例如：
    - 物流操作
    - 纯制造
    - 集成电路硬件
    - 航天控制
    - 汽车制造
    - 化工
    - 通信工程
    - 与数据、统计、金融科技无明显关系的岗位

首版 Demo 可先抓取 WonderCV 前若干页，生成真实岗位库。岗位数量不重要，真实性优先；宁可少，也不能编造。

## 五、可选用户确认岗位

如果我提供了已确认真实岗位，可以作为人工确认数据加入 `JOBS_DATA`，但必须标注真实来源。

示例格式：
- 公司名称：[如：中国人保]
- 公司类型：[央企国企 / 互联网大厂 / 事业单位 / 其他]
- 岗位名称：[岗位全称，含岗位编号]
- 工作城市：[城市]
- 发布时间：[YYYY-MM-DD 或 待核验]
- 投递截止：[YYYY-MM-DD 或 待核验]
- 投递链接：[官网或可核验链接]
- 信息来源：[如：公司官网 / 用户确认 / WonderCV]
- 状态：[可投 / 待核验 / 已截止]
- 背景匹配：[与我的学历、技能、实习经历的具体匹配点]

## 六、网页发布

Demo 调试完成后，调用目标 Agent / 托管平台提供的网页发布能力，发布为公网链接。

发布要求：
- 输出可访问公网链接
- 页面刷新后，岗位库能从 HTML 内嵌数据或外部持久岗位数据源恢复
- 投递记录、待投递清单状态、目标公司、备注、自定义列等用户操作数据，在不接外部数据库时只承诺当前浏览器 `localStorage` 持久化
- 必须在页面说明中明确：如果当前平台不支持在线数据库，跨设备同步需要外部持久数据源；当前浏览器数据可通过 CSV 导出备份
- 不要把目标托管平台动态服务的实例文件系统写成“云端数据库”或“跨设备同步”
- 若接入外部持久数据源，确保密钥、飞书凭证、GitHub token 等敏感信息不出现在前端源码中

## 七、每日定时自动采集更新任务

设置每日早上 8 点自动执行，时区为 `Asia/Shanghai`。

### 定时任务运行限制

如果目标平台的定时任务是 isolated session，历史执行记录可能每次都是不同 Session ID。任务运行在全新工作区时，可能找不到原会话里的 HTML 文件路径，因此不能采用“读取原工作区 `index.html` → 修改 `JOBS_DATA` → 重新发布”的方案。

禁止方案：
- 定时任务依赖 `/workspace/[旧 session]/.../index.html`
- 定时任务修改本地 HTML 后假设能自动影响线上页面
- 定时任务把岗位数据写入实例本地 JSON 文件后声称网页跨实例可读取
- 飞书日报用新数据，但网页端继续使用旧 `JOBS_DATA`

推荐方案按优先级选择：
1. 使用外部持久岗位数据源：定时任务抓取岗位后写入飞书多维表格、飞书电子表格、GitHub 仓库/Gist、Supabase 等；网页端启动时从该数据源读取最新岗位。
2. 使用可靠线上岗位 API：只有当该 API 背后连接外部持久存储时才可使用。若 API 只是平台实例本地文件，不可作为长期数据源。
3. 使用官方页面发布/更新能力：如果目标平台提供可在定时任务中直接更新线上页面资产的能力，可以调用官方能力更新岗位数据并发布；不要依赖旧 session 文件路径。
4. 如果以上都不可用：只发送飞书日报，不承诺网页端自动同步；在交付说明中明确网页端需手动更新或改接外部持久数据源。

任务流程：
1. 抓取 WonderCV 央企国企页和全量页，必要时翻页。
2. 搜索过去 24 小时新发布或更新的岗位。
3. 严格按「四、数据来源与采集规则」执行：毕业年份、城市、背景匹配、去重、真实性校验。
4. 为本次任务写入的每条岗位追加 `scraped_date = 今天日期（YYYY-MM-DD）`。
5. 将最新岗位数据直接 POST 到线上服务器 API，或写入外部持久岗位数据源，或通过官方发布能力更新线上页面资产；禁止依赖旧 session 文件路径。
6. 前端从同一份最新岗位数据源/API 加载数据后，必须同步刷新今日新增、岗位总库、待投递清单和数据复盘。
7. 今日新增只按 `scraped_date` 判断过去 24 小时新增，不能按 `publish` 判断。
8. 保留用户操作数据，不要覆盖投递进度、目标公司、备注、自定义列等状态数据；这些用户状态默认在本机 `localStorage`，除非已明确接入外部数据库。
9. 发送飞书日报。
10. 输出采集日志：新增数、删除/剔除原因、待核验数、写入目标、网页端同步方式、飞书推送结果。

定时任务必须再次强调：
- 只收录 `[毕业年份]` 及以后的秋招岗位
- 只保留 base 包含 `[意向城市]` 的岗位
- 不编造任何岗位、截止时间或链接
- 截止时间未知排到待投递清单最后
- 今日新增必须基于 `scraped_date`，不是 WonderCV 原始 `publish`
- 定时任务不能依赖旧 session 工作区文件路径
- 飞书日报和网页端必须使用同一份岗位数据源；如果网页无法同步，必须明确说明失败原因

## 八、飞书日报推送

使用飞书应用凭证推送日报到指定群：
- 飞书 App ID：[你的飞书 App ID]
- 飞书 App Secret：[你的飞书 App Secret]
- 推送群 Chat ID：[飞书群 Chat ID]

飞书 API：
- 获取 token：`POST https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal`
- 发送消息：`POST https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id`

每日岗位采集完成后，自动发送 interactive 卡片消息。若网页端已成功同步最新岗位数据，在卡片中说明“网页已更新”；若网页端因缺少外部持久数据源或发布能力无法同步，必须在卡片中说明“仅飞书日报已更新，网页端未自动同步”。卡片内容包含：
1. 今日新增岗位清单：公司 + 岗位 + 城市 + 截止时间 + 投递链接，仅限意向城市
2. 即将截止岗位预警：3 天内截止优先预警；截止时间待核验单独列出
3. 当日优先投递建议：基于背景匹配度和截止紧迫度推荐 3-5 个岗位，并说明理由
4. 工作台公网链接
5. 数据质量说明：新增岗位中「可投」和「待核验」分别多少条

首次配置完成后，先发送一条启动通知卡片验证飞书连通性。

## 九、后续网申助手模式（可选，不自动提交）

不要设计成全自动投递或自动提交网申。现实约束包括登录、验证码、文件上传、复杂动态表单和人工确认。

可作为后续扩展的「网申助手」模式：
- 从工作台读取选中岗位信息和 JD
- 根据岗位类型判断简历版本：央国企用「央国企版」简历，互联网用「互联网版」简历
- 打开网申页面
- 对标准字段提供预填建议
- 根据简历和 JD 生成开放题回答草稿
- 暂停等待我检查，最终提交必须由我手动完成

## 十、交付目标

最终交付：
- 可预览前端 Demo
- 可访问公网工作台链接
- 6 个 Tab 功能完整可用
- 真实岗位数据源；首版 HTML 内嵌 `JOBS_DATA`，如需定时自动网页更新则接入外部持久岗位数据源或官方发布能力
- 投递进度双表、JD 弹窗、自定义列、CSV 导入导出、本机 `localStorage` 持久化说明；如需跨设备云同步，必须额外交付外部持久数据源方案
- 每日 8:00 自动采集更新任务
- 飞书日报 interactive 卡片推送
- 后续可用自然语言继续迭代筛选关键词、去重逻辑、城市规则、岗位方向和 UI

验收标准：
- 页面首屏能看到个人背景概览和 6 个 Tab
- 岗位总库每条记录都有真实来源或明确「待核验」
- 非意向城市岗位不会进入岗位库
- 编造数据为 0
- 待投递清单能按截止时间排序，`待核验` 排最后
- 点击 `已投递` 能进入对应投递表
- 投递记录在同一浏览器刷新后不丢失；跨设备不承诺同步，除非已接入外部持久数据源
- 飞书群能收到启动通知和日报
- 飞书日报新增岗位与网页端岗位数据源一致；如果网页端无法自动同步，交付说明必须明确原因和后续接入方案
