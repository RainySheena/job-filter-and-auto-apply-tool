# 秋招投递作战工作台 — 一键搭建 Prompt

> 直接复制以下 Prompt 发给千问办公(QwenWork) Agent，用于一次性搭建当前版本的秋招岗位投递作战工作台。
> 请先把所有 `[占位符]` 替换为你自己的信息。不要把飞书 App Secret、群 ID、手机号、简历文件等敏感信息写入公开网页代码。

---

## 角色

你是一名全栈开发工程师，使用 Vibe-Coding 模式，帮我搭建一个可公开访问、可持续自动更新的秋招岗位投递作战工作台。要求先交付可预览网页 Demo，确认后发布到 QW Pages，并配置每日自动岗位采集和飞书日报推送。

必须严格遵守：**岗位数据只能来自真实可核验来源，禁止编造公司、岗位、发布时间、截止时间、投递链接或 JD。无法确认的信息统一标记为「待核验」。**

## 一、个人背景（用于岗位筛选与匹配说明）

- 姓名：[你的姓名]
- 学历：[如：XX大学 统计学硕士 / XX大学 经济学本科]
- GPA：[如：4.0/4.0，排名 X/Y]
- 实习经历：[如：字节跳动-数据科学]
- 核心技能：[如：Python / SQL / R / Spark / A/B 实验 / 因果推断 / 机器学习 / 统计检验]
- 毕业年份：[如：2027届]
- 投递方向：[如：互联网数据分析 / 数据科学 / 数据挖掘]
- 意向城市：[如：北京]
- 城市硬约束：只收录工作城市/base 包含 `[意向城市]` 的岗位；其他城市一律剔除。若一个岗位城市列表同时包含意向城市和其他城市，可以保留，但必须在城市字段完整展示全部城市。

## 二、网页整体要求

搭建单页工作台，采用 **顶部导航 Tab + 分屏面板切换** 布局。点击导航 Tab 切换面板，同一时间只展示一个板块。

页面顶部必须包含：
- 标题：秋招投递作战工作台
- 当前日期与秋招状态，例如：`YYYY-MM-DD · 秋招进行中`
- 个人背景概览栏：姓名、学历、GPA、技能标签、实习经历、投递方向、意向城市
- 风格：简洁清爽的求职工具风，白底、浅色卡片、清晰表格、链接可点击跳转，适配电脑浏览器

必须包含 6 个 Tab：
1. 今日新增
2. 岗位总库
3. 待投递清单
4. 投递进度管理
5. 目标公司池
6. 数据复盘面板

## 三、功能模块

### 1. 今日新增岗位

展示过去 24 小时新收录或更新的秋招岗位。

必须包含：
- 统计卡片：今日新增岗位数、其中可投数、涉及公司数
- 筛选器：公司类型、招聘批次
- 表格字段：公司名称、岗位名称、工作城市、招聘批次、发布时间、投递截止、官网链接、岗位状态
- 新增岗位在公司名旁标记 `NEW`
- 官网链接按钮文案为 `去投递 ↗`
- 若截止时间未知，展示 `待核验`，不要生成虚假的截止日期

### 2. 岗位总库

展示全部收录岗位。岗位总库是网页唯一岗位数据源，代码中使用 `JOBS_DATA` 数组维护。

筛选器必须支持：
- 公司
- 公司类型
- 城市
- 招聘批次
- 岗位状态
- 重置筛选

表格字段必须为：
公司名称 | 公司类型 | 岗位名称 | 工作城市 | 背景匹配 | 官网链接 | 发布时间 | 投递截止 | 信息来源 | 岗位状态

功能要求：
- 支持导出岗位总库 CSV
- 所有链接必须指向真实可核验来源：WonderCV 详情页、WonderCV 可检索列表页、公司官网招聘页或用户确认的真实岗位页面
- `背景匹配` 列必须说明岗位与个人背景的具体契合点，例如「统计学+机器学习契合」「因果推断+A/B 实验契合」「经济金融+统计背景契合」
- 若岗位方向与个人背景不匹配，直接剔除，不要为了凑数量保留
- 页面说明文案必须明确：仅收录 base 在 `[意向城市]` 的岗位，数据来自真实抓取或用户确认，无法确认则标记「待核验」

### 3. 待投递清单

从岗位总库中自动筛出未处理、未截止、仍可跟进的岗位，按截止时间升序展示；截止时间为 `待核验` 的岗位排在最后。

必须包含：
- 统计卡片：待投递总数、3 天内截止数、最近截止岗位
- 卡片式列表：公司名称、岗位名称、城市、倒计时、公司类型标签、投递链接
- 倒计时规则：3 天内红色预警，7 天内橙色提醒，其余普通展示；`待核验` 显示「截止时间待核验」
- 每张卡片有三个操作：`不投递`、`已投递`、`去投递 ↗`
- 点击 `不投递` 后，该岗位从待投递清单移除，并记录为不投递状态
- 点击 `已投递` 后，该岗位从待投递清单移除，并自动加入投递进度管理；分类根据公司类型自动归入「央国企」或「互联网」

### 4. 投递进度管理

这是核心模块。必须做成双表分区：
- 央国企投递
- 互联网投递

表格基础字段：
公司名称 | 岗位信息 | JD | 投递时间 | 目前进度 | 笔试 | 1面 | 2面 | 操作

字段和交互要求：
- `岗位信息` 支持填写完整岗位名称和岗位编号，例如 `集团公司-科技运营岗-2027届校招(J78974)`
- `JD` 以按钮 `📄 查看JD` 展示，点击打开弹窗，可查看和编辑完整 JD 文本
- `目前进度` 是下拉框，选项固定为：简历初筛、笔试、面试、OC、挂掉
- 投递时间、笔试、1面、2面等字段支持内联编辑
- 操作列包含：编辑、复制、删除
- 支持 `+ 添加投递记录`，弹窗字段至少包含：公司分类、公司名称、岗位信息、投递时间、目前进度、JD
- 添加记录时可选择分类：央企国企 / 互联网，自动归入对应表
- 支持 `+ 添加列`，用户可添加自定义列；表头展示列名并允许删除自定义列
- 支持导出投递记录 CSV
- 支持导入投递记录 CSV
  - 导入分类选项：使用 CSV 中的分类（无则默认央国企）、全部导入为央国企、全部导入为互联网
  - 导入方式：覆盖导入、增量添加
  - 增量添加按 `公司名称 + 岗位信息` 去重
- 支持行拖拽排序或至少保留拖拽手柄视觉位
- 数据实时保存：优先写入 QW Pages / QwenWork 可用的服务端状态接口（例如 `/api/state`）；同时写入浏览器 localStorage 做兜底，实现本地 + 云端双写。若服务端状态接口不可用，必须保证 localStorage 可用。

### 5. 目标公司池

卡片式管理重点关注公司。

必须包含：
- 添加公司输入框和 `添加公司` 按钮
- 每张公司卡片展示：公司名称、关注理由、岗位库收录数量、可投数量、删除按钮
- 岗位库收录数量和可投数量必须自动从 `JOBS_DATA` 联动计算
- 允许保留目标公司即使岗位库当前没有收录岗位，用于后续持续追踪
- 默认目标公司可按个人背景初始化，例如互联网数据科学目标、央国企金融科技目标、统计/精算/量化相关目标

### 6. 数据复盘面板

随岗位库和投递记录实时更新。

核心指标：
- 收录岗位数
- 已投递数
- 面试率
- OC 数
- 已截止数

图表和列表：
- 各阶段岗位分布条形图：简历初筛、笔试、面试、OC、挂掉
- 岗位状态占比环形图或圆环式视觉组件：可投、待核验、已截止
- 投递漏斗图：收录岗位 → 已投递 → 进入笔试/面试 → 拿到 OC
- 关键转化率列表：投递率、笔试/面试率、OC 率、流程终止占比

## 四、数据来源与采集规则

岗位数据优先来自 **超级简历 WonderCV 校招信息汇总** 实抓：
- 央企国企：`https://www.wondercv.com/xiaozhao/?graduate_year=[毕业年份]及以后&company_nature=央企国企`
- 全量/互联网：`https://www.wondercv.com/xiaozhao/?graduate_year=[毕业年份]及以后`

可以补充的真实来源：
- 公司官网招聘页
- 用户明确确认的真实岗位信息
- 用户后续提供的 CSV、飞书表格或多维表格数据

硬性采集规则：
1. 只收录 `[毕业年份]` 及以后的秋招、校招、提前批岗位。
2. 批次字段统一为 `[毕业年份]秋招` 或 `[毕业年份]秋招提前批`。
3. 只保留工作城市/base 包含 `[意向城市]` 的岗位，其他城市一律剔除。
4. 根据 `公司名称 + 岗位名称 + 工作城市 + 招聘批次` 四个维度去重。
5. 每条岗位尽量抓取：公司名称、公司类型、岗位名称、工作城市、背景匹配、官网链接、发布时间、投递截止、信息来源、岗位状态。
6. 无法确认投递有效性的岗位，状态标记为「待核验」。
7. 无法确认截止时间，截止字段填「待核验」。
8. 禁止用经验补全、编造、猜测或美化岗位名称。例如 WonderCV 原文没有写「数据分析」时，不能强行改成「数据分析岗」。
9. 大厂官网如果是 JavaScript 动态渲染、无法直接抓取岗位列表，不要编造岗位；改用 WonderCV 可核验信息或标记「待核验」。
10. 如果 WonderCV 详情页不可访问，但 WonderCV 列表页可以检索到公司和岗位线索，可以链接到可检索列表页，并将状态标为「待核验」。
11. 按个人背景筛选匹配方向，保留：
    - 数据分析
    - 数据科学
    - 数据挖掘
    - 统计学
    - 量化
    - 金融科技
    - 投资研究
    - 管培生（科技/金融/数据方向）
    - 精算
    - AI 算法
    - 大数据 / 数据仓库 / 数据架构
12. 剔除不匹配方向，例如：
    - 物流操作
    - 纯制造
    - 集成电路硬件
    - 航天控制
    - 汽车制造
    - 化工
    - 通信工程
    - 与数据、统计、金融科技无明显关系的岗位

首版 Demo 可先抓取 WonderCV 前若干页，生成真实岗位库。岗位数量不重要，真实性优先；宁可少，也不能编造。

## 五、可选用户确认岗位

如果我提供了已确认真实岗位，可以作为人工确认数据加入 `JOBS_DATA`，但必须标注真实来源。

示例格式：
- 公司名称：[如：中国人保]
- 公司类型：[央企国企 / 互联网大厂 / 事业单位 / 其他]
- 岗位名称：[岗位全称，含岗位编号]
- 工作城市：[城市]
- 发布时间：[YYYY-MM-DD 或 待核验]
- 投递截止：[YYYY-MM-DD 或 待核验]
- 投递链接：[官网或可核验链接]
- 信息来源：[如：公司官网 / 用户确认 / WonderCV]
- 状态：[可投 / 待核验 / 已截止]
- 背景匹配：[与我的学历、技能、实习经历的具体匹配点]

## 六、网页发布

Demo 调试完成后，调用千问办公「我的网页」能力（QW Pages）发布为公网链接。

发布要求：
- 输出可访问公网链接
- 页面刷新后数据仍可用
- 投递记录、目标公司、备注、自定义列等用户操作数据可持久化
- 若使用服务端状态接口，确保密钥、飞书凭证等敏感信息不出现在前端源码中

## 七、每日定时自动采集更新任务

设置每日早上 8 点自动执行，时区为 `Asia/Shanghai`。

任务流程：
1. 抓取 WonderCV 央企国企页和全量页，必要时翻页。
2. 搜索过去 24 小时新发布或更新的岗位。
3. 严格按「四、数据来源与采集规则」执行：毕业年份、城市、背景匹配、去重、真实性校验。
4. 更新公网工作台中的 `JOBS_DATA` 数组。
5. 重新发布 QW Pages 页面。
6. 保留用户操作数据，不要覆盖投递进度、目标公司、备注、自定义列等状态数据。
7. 输出采集日志：新增数、删除/剔除原因、待核验数、发布结果。

定时任务必须再次强调：
- 只收录 `[毕业年份]` 及以后的秋招岗位
- 只保留 base 包含 `[意向城市]` 的岗位
- 不编造任何岗位、截止时间或链接
- 截止时间未知排到待投递清单最后

## 八、飞书日报推送

使用飞书应用凭证推送日报到指定群：
- 飞书 App ID：[你的飞书 App ID]
- 飞书 App Secret：[你的飞书 App Secret]
- 推送群 Chat ID：[飞书群 Chat ID]

飞书 API：
- 获取 token：`POST https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal`
- 发送消息：`POST https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=chat_id`

每日岗位采集、网页更新完成后，自动发送 interactive 卡片消息，内容包含：
1. 今日新增岗位清单：公司 + 岗位 + 城市 + 截止时间 + 投递链接，仅限意向城市
2. 即将截止岗位预警：3 天内截止优先预警；截止时间待核验单独列出
3. 当日优先投递建议：基于背景匹配度和截止紧迫度推荐 3-5 个岗位，并说明理由
4. 工作台公网链接
5. 数据质量说明：新增岗位中「可投」和「待核验」分别多少条

首次配置完成后，先发送一条启动通知卡片验证飞书连通性。

## 九、后续网申助手模式（可选，不自动提交）

不要设计成全自动投递或自动提交网申。现实约束包括登录、验证码、文件上传、复杂动态表单和人工确认。

可作为后续扩展的「网申助手」模式：
- 从工作台读取选中岗位信息和 JD
- 根据岗位类型判断简历版本：央国企用「央国企版」简历，互联网用「互联网版」简历
- 打开网申页面
- 对标准字段提供预填建议
- 根据简历和 JD 生成开放题回答草稿
- 暂停等待我检查，最终提交必须由我手动完成

## 十、交付目标

最终交付：
- 可预览前端 Demo
- 可访问 QW Pages 公网工作台链接
- 6 个 Tab 功能完整可用
- 真实岗位数据源和 `JOBS_DATA` 数组
- 投递进度双表、JD 弹窗、自定义列、CSV 导入导出、状态持久化
- 每日 8:00 自动采集更新任务
- 飞书日报 interactive 卡片推送
- 后续可用自然语言继续迭代筛选关键词、去重逻辑、城市规则、岗位方向和 UI

验收标准：
- 页面首屏能看到个人背景概览和 6 个 Tab
- 岗位总库每条记录都有真实来源或明确「待核验」
- 非意向城市岗位不会进入岗位库
- 编造数据为 0
- 待投递清单能按截止时间排序，`待核验` 排最后
- 点击 `已投递` 能进入对应投递表
- 投递记录刷新后不丢失
- 飞书群能收到启动通知和日报
